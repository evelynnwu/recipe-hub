# Prevent platform-specific optional dependencies from being saved to package.json
save-optional=false

# Use exact versions to prevent dependency drift
save-exact=true

# Skip platform-specific optional dependencies during install
optional=false